# --- Core ---
DATABASE_URL="file:./dev.db"
NEXTAUTH_SECRET="replace-with-a-random-string"
NEXTAUTH_URL="http://localhost:3000"

# --- Media server (images + video uploads) ---
# Self-hosted on a Contabo VPS (media.mompuffs.com): MinIO storage + a Node
# service that resizes images and transcodes video. This app only ever
# issues a short-lived signed upload token from the server -- the browser
# uploads the actual file bytes directly to the media server, so large
# uploads never pass through a Vercel function.
MEDIA_SERVICE_URL="https://media.mompuffs.com"
MEDIA_SERVICE_INTERNAL_SECRET=""

# --- Print-on-demand integrations ---
# Each shop connects its own Printify/Printful/Peaprint account from its
# dashboard's Connections page (src/app/dashboard/shop/connections) -- stored
# encrypted per shop in the database, with no shared/global fallback. No env
# vars needed here.

# --- Sale notification emails ---
# resend.com > API Keys. Without this set, sale notification emails are
# silently skipped (a warning is logged) -- everything else still works.
RESEND_API_KEY=""
# Optional: defaults to Resend's no-setup-required sandbox sender. Switch to
# a verified "you@mompuffs.com" address once that domain is set up in Resend.
RESEND_FROM_EMAIL="Mompuffs <onboarding@resend.dev>"
